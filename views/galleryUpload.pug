extends includes/layout

block content
    .modalContainer
        .modalWrapper
            .croppperContainer
            #closeBtn 취소

           
    .container
        form(action="/gallery/upload" method="post" enctype="multipart/form-data").uploadContainer
            input(type="hidden" name="_csrf" value=csrfToken)
            input(type="hidden" name="address")
            input(type="hidden" name="location")
            h3 사진 업로드
            .flexContainer
                .flexContainer__item
                    span 타이틀
                .flexContainer__item
                    input(type="text" name="title" placeholder="ex) 하버 몸 만들어드립니다")
            .flexContainer
                .flexContainer__item
                    span 사진
                .flexContainer__item
            input(type="file" id="cropperFile" name="photos" multiple)
            #galleryPreview
            
            input(type="submit" value="생성").btn.primary
    script(src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js") 
    script(src="/static/js/cropper.js") 
